services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    depends_on:
      - postgres

  postgres:
    image: postgres:18
    env_file:
      - ./backend/.env
    environment:
      POSTGRES_DB: apla_dev
    ports:
      - "5432:5432"